import React from "react"
// This component is called when the search for user info is initiated 
// and terminated when the results are fetched.
export default function Loading() {
  return (
    <div style={{ padding: "30px" }}>
      <h1>Loading...</h1>
    </div>
  )
}
